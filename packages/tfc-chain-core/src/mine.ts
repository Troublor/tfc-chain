import {TransactionConfig} from './defines';
import * as ethers from 'ethers';

export async function submitSector(afid: string, merkleRoot: string, config: TransactionConfig) {

}

export async function submitSeed(afid: string, config: TransactionConfig) {

}

export async function submitSeedComment(seed_afid: string, like: boolean, config: TransactionConfig) {

}